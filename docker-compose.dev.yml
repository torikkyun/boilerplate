services:
  mysql:
    image: mysql
    container_name: mysql_dev
    restart: always
    ports:
      - '3307:3306'
    env_file:
      - .env.development
    volumes:
      - ./mysql_data:/var/lib/mysql

  # postgres:
  #   image: postgres
  #   restart: always
  #   ports:
  #     - '5433:5432'
  #   env_file:
  #     - .env.development
  #   volumes:
  #     - ./postgres_data:/var/lib/postgresql/data

volumes:
  mysql_data:
  # postgres_data:
