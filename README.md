# nest-boilerplate

## Cài đặt dự án

```bash
pnpm add -g degit
degit torikkyun/nest-boilerplate skibidi
cd skibidi
pnpm install
docker-compose -f docker-compose.dev.yml up -d --build
```

## Chạy dự án

```bash
pnpm start:dev
```
